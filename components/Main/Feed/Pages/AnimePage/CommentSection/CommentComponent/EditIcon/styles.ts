import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import styled from 'styled-components'

export const EditHoverable = styled(FontAwesomeIcon)`
&:hover {
  cursor: pointer;
  color: #4FE3FF;
  transition: 0.25s;
}
`
